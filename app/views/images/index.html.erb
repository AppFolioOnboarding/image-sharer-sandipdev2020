<h3>Welcome to image app</h3>
<div class="parent-div">
<% @images.each do |image| %>
  <label>
    <%= image.id %>
  </label>
    <img src="<%= image.url %>" class="image-small">
  <br>
<% end %>
</div>
<%= link_to 'Add image', new_image_path %>
